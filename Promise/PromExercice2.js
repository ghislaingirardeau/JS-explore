/* 
1- Afficher dans la console "Charge le serveur" au début du code
2- Créer la promise dans une fonction qui se résoud et renvoie :
    - "Connection à la BDD terminés" si le paramètre de la fonction est true
    - "Connection à la BDD terminés a échouée" si le paramètre de la fonction est false
4- Afficher dans la console le résultat de la promise si on lui envoie true ou si on lui envoie false :
    1er cas en utilisant then/catch
    2eme cas en utilisant async/await
5- Afficher dans la console "Connection à la BDD en cours"

Tips: on utilisera setTimeout() dans la promise, ainsi que then et catch
*/
