/* 
1- Afficher dans la console "Charge le serveur" au début du code
2- Créer 2 promises (pA, pB) dans une fonction qui se résoud et renvoie chacune :
    - "Connection à la BDD terminés" si le paramètre de la fonction A est true
    - "Connection à la BDD terminés a échouée" si le paramètre de la fonction A est false
    - "Récupèration des données terminés" si le paramètre de la fonction B est true
    - "Récupèration des données a échouée" si le paramètre de la fonction B est false
4- Afficher dans la console les résultats des promises EN UTILISANT ASYNC/AWAIT suivant si on leurs envoie true ou false
5- Afficher dans la console "Chargement en cours", doit etre ecrit en dernier

Tips: on utilisera setTimeout() dans la promise
*/

